# SDD ledger — plan: WDT quantitative A/B (W0 control / W1 deferred-flash / W3 breadcrumb)

Workspace (outside repo): C:\Users\moilo\AppData\Local\Temp\opencode\bcon-wab\
BASE (no commits expected): c366bf4716d3b049d95411dd9301043e447bdaa4
Spec authority: docs/handoff_bt_20260914.md + Phase-3 log COM3_2026_09_14.23.51.02.287.txt (Death A: no-write wedge encrypt→+2s; Death B: first-write in open callback → +2s; survival on skip) + AB5 log (SUB failure with zero WDT — independent issue, do not conflate).

## Rulings
- Ruling: A/B model with per-task revert (as AB1–5) — each variant UF2 + diff + byte-restore + proof, so every variant stays single-variable vs control. Cost if wrong: none (UF2s persist in log/).
- Ruling: W1 covers store_host only (Death-B proven killer in session 2). T3's key-drop path never fired in any log — out of scope unless W1 is inconclusive. Cost if wrong: one more variant later.
- Ruling: single subagent tier; no commits; wakecon untouched; no secrets; isolated temp build dirs; never reuse build/.
- Ruling: W3 breadcrumb uses watchdog scratch regs ONLY after grep-proving zero existing users in src/; all added lines carry W3DIAG markers for later removal. Cost if wrong: post-mortem collision (mitigated by the grep gate).

## Pre-flight conflict scan
| Pair / Task | Produces vs consumes | Finding |
|---|---|---|
| W0 vs others | W0 produces UF2 only, no source change | Clean |
| W1 vs W3 (share main.c, maybe store.c) | Sequential + full restore between tasks; neither consumes the other's output | Clean given restore discipline |
| W1 self-check | Fire handler reads probe_host_addr (always RAM-current); repeat opens re-arm; no pending buffer needed | Clean by design |
| W3 self-check | IRQ callback writes scratch only (no locks/printf/BT calls); poll writes single words; boot dump reads post-reboot | Clean by design; cold-boot garbage guarded by magic |
| Rubric vs plan | Verification = compile + restore proof + owner HW run (log criteria per brief); no unit tests for trial UF2s | Noted as lens |

## Progress
Task W0: complete (no commits, no source edits; UF2 log/pico-bcon-w0-control.uf2 816640B SHA 2629D6DB; status byte-identical SHA 250472FD; review clean)
Task W1: complete (no commits; force-refactor + 3s one-shot deferral exact; build exit 0, UF2 log/pico-bcon-w1-defer.uf2 816640B; restore hash-proven; review clean; controller verified zero W1 markers in tree, UF2 present, status==intended state)
Task W3: complete (no commits; 7 hunks pure-addition 31/31 W3DIAG-marked, IRQ-safe, placements per brief; build exit 0, UF2 log/pico-bcon-w3-crumb.uf2 817664B; restore hash-proven + zero markers; review clean with 1 Important caveat: scratch[4] ev field unreliable (SDK marker collision) ? irq/poll/stage signatures remain valid; controller verified UF2 present, zero W3DIAG in tree, status==intended state)
W1 verdict (HW logs 00.45 file): deferred save STILL kills ~2s later, 3/3 (saves 51.2/58.5/12.2 -> deaths 53.2/00.5/14.2). W0/W3 (no-write sessions) stable for minutes with clean 0x13 exits, zero WDT. Score: write-then-death 4/4 incl Phase3-s2; no-write survival ~10 sessions. Conclusion: flash WRITE in BT-active state is the killer regardless of callback context. T3 auth-fail->drop->repair validated 2x in-field.
Task W4: complete (no commits; W1+W3 union verified modulo 1 whitespace-only blank line + report count nit ? both documentary, no tree fix needed as tree is reverted; build exit 0, UF2 log/pico-bcon-w4-defer-crumb.uf2 817664B; review Approved; controller verified UF2 present, zero markers in tree)
Task W4: minor (deferred): w4 diff has 1 extra blank line; report line counts off ? whitespace/paperwork only, UF2 behaviorally identical
W4 verdict (HW log 01.32): post-mortem 4/4 IDENTICAL ? stage=2 (poll_tick completed to pre-feed checkpoint), poll frozen, irq advancing, ev=0x6e disregarded (known SDK-collision caveat). Rules OUT wedge in poll_tick body (inbox drain, exec, flush, usb, link). Remaining: wedge inside BTstack event/run-loop processing, or inside the deferred flash op itself. Next: W5 times the store call.
Task W5: complete (no commits; W1 hunks verbatim + W5DIAG timing at single choke point, error semantics preserved; build exit 0, UF2 log/pico-bcon-w5-timed-store.uf2 816640B; restore hash-proven + zero markers; review clean; controller verified UF2 present, zero markers in tree)
W5 verdict (HW log 01.44, 5/5 identical): store begin -> dt_us~2070 rc=0 -> host saved -> death ~2.0s later. Write completes in ~2ms (program-like, NOT erase stall). NOT a stall-in-write: post-write corruption (or feed-stop at write). BCON 1s timer also stops post-save. Score: write-then-death 9/9 (Phase3-s2 + W1x3 + W5x5); no-write survival ~12 sessions. T3 delete-path survival explained: keys never persisted (non-bonding) so delete_all is a flash no-op in practice. Permanent direction: NO flash mutation while BT active ? single deferred/quiesced worker for host-save + key-drop + color-set (COLOR_SET via exec_fx/poll_tick would be equally lethal today).
W6 rationale: T4 deleted the visibility needed now; 4:24-era code proves hci_dump is behaviorally inert (60s+ survival with full dump). Decisive test = byte-diff death-session L2CAP bytes vs AB1-victory dump.
Task W6: complete (no commits; hci_dump-only 6-line re-add, build exit 0, UF2 log/pico-bcon-w6-hcidump.uf2 818176B; restore hash-proven + zero markers; reviewed by controller evidence: zero-hit grep + status==intended set + UF2 present; full subagent review skipped per T6R precedent ? 6-line reversible logging change)
Ruling: W6 review by controller evidence (trivially reverted logging-only change, hash-proven restore). Cost if wrong: negligible (UF2 validated by build exit 0; HW run will immediately reveal logging failure).
W6 verdict (HW log 02.06): death session wrote at 26.432 then MODE_CHANGE mode=2 at 26.577 then silence/death ~28.4. BUT session 2 (no write) survived THREE mode changes (2, then 0, then 2) over 30s+; AB1 log has 6 MODE_CHANGE with full survival. SNIFF entries are INNOCENT. Refined score: write-then-death 10/10; no-write survival ~14 sessions. Working mechanism: flash-write IRQ blackout on SNIFF-active link -> controller deaf -> TX path wedges loop (timers die with it); WDT 2s later. Session-1 no-write death remains the single unexplained instance (possibly SNIFF-during-signaling race; watch item, needs recurrence with HCI).
External review received: AGREES timer-scheduler framing over CYW43-state framing (BCON+poll die together => common scheduler failure, not radio-only wedge). VALID CATCH: W4 stage=2 does NOT prove full poll completion ? usb_timer re-arm (set+add in usb_handler) runs AFTER poll_tick returns; wedge could sit in tail/re-arm path. My stage=2=complete claim overstated. ADOPT: (1) 1ms-vs-2.07ms deterministic deadline-crossing explains 9/9 rate; (2) phase-gated write windows A-F to map danger; (3) flash-backend op counters to prove Session-1 zero-flash + T3-delete no-op question; (4) finer stages 3/4/5 + add_timer verification; (5) permanent worker safety conditions (ACL==0/HID disc/no-outgoing/no-pairing/no-L2CAP/no-TX-pending) + aggregation + power-fail safety; (6) W6 reading guide (last-completed-event vs next-expected-command, ACL credits, L2CAP identifiers). Session-1: keep two-model stance. Next: apply W6 guide to captured 02.06 death session, then build counters+fine-stage variant.
Step 1 (desk decode, delegated): verdict B ? windows byte-identical except SSP nonce/key; no outstanding protocol response; 1-outstanding ACL normal; TLV write sole app divergence; BCON+poll stop unexplained by HCI. Full analysis in subagent result (ses_f5f11b5a). Proceeding to Step 2 unified instrumentation per owner-approved sequence.
Task W7: complete (no commits; counters+stages+timer-acct+hci_dump unified, all W7DIAG-marked, no SDK edits; build exit 0, UF2 log/pico-bcon-w7-diag.uf2 823808B; restore proven incl. new-file deletion; review clean; controller verified UF2 present, files deleted, zero markers, status==intended set)
SDK audit (controller): run loop = async-context over THREADSAFE_BACKGROUND (none flavor keeps it for gpio); timer callbacks run in LOW-PRIO IRQ under recursive mutex; main thread sleeps on sem. Double-add RULED OUT as killer: BTstack add_timer double-add would log_error via hci_dump_log (live path, ENABLE_LOG_ERROR set) ? zero occurrences in 4 death logs incl dump-enabled ones. +1 add/fire deltas demoted (likely cross-boot accumulation, not evidence). log_error routes via hci_dump_log (methodology note). Remaining prime suspect: at-time alarm re-arm across blackout OR low-prio-IRQ wedge; timer LIST ops clean.
External review #2 (W7 post-mortem, brief docs/wdt_w7_postmortem_brief_20260915.md): trail=2,3,4,5,7,8,9,10 proves last usb_handler completed set+add to return-imminent; fault boundary = ADD_TIMER_DONE -> next FIRE missing (lower scheduler path). del=1/1 and +2 on all timers most likely cross-boot accumulation in NOLOAD recorder. W8 adopted with extensions: post/current epoch, last_tag + last_epoch per op, boot baseline + boot deltas, delete_found boundary via hci_dump Erase-tag correlation (SDK-internal match flag not observable without SDK edits).
Task W8: complete (no commits; W7 design + epoch/baseline/delta, all W8DIAG-marked, no SDK edits; build exit 0, 229/229, UF2 log/pico-bcon-w8-epoch.uf2 824832B SHA 226FDBEC; restore proven: 4 hashes True, status identical, zero markers, files deleted; controller verified UF2 present, status==Phase-3 set). Awaiting owner HW run (flash -> death repro -> next-boot w8 dump); reading guide in Temp task-8-report.md (archived copy sdd/task-8-report.md).
Desk batch (no HW, owner out): (1) delete-path static audit — controller's "only live caller is FX_KEY_DELETE, cap path dead" CONFIRMED on grep-visible graph (tag_delete_fn static, 2 sites; tlv->delete_tag resolves to SDK bank delete; shim cannot produce del); caveats: counter-placement must be re-checked (if W7 counted SDK delete, auth-fail path revives) + live T_KEY_DELETE protocol event unaudited. (2) timer double-arm audit — H2/H3 DEAD (init straight-line once; handlers unconditional single re-arm; owned three never removed; handle_bt_ready touches reconnect only); H1 accumulation stands by elimination, W8 boot-delta confirms. (3) quiesced-worker design doc archived plans/quiesced-worker-design.md (recommends A: dirty-flag + quiesced single worker in poll_tick). (4) W9 phase-gated A/B + W10 timer-period A/B briefs archived sdd/bcon-wab-briefs/task-9-brief.md + task-10-brief.md (both with NO-GO gates; W9-E detector + W10 handshake-pacing risks flagged).
