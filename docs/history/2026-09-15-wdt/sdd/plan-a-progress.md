# SDD ledger — plan: C:\pico-bcon\docs\superpowers\plans\2026-09-15-plan-a-telemetry-v4.md

BASE (HEAD at start, no commits expected — handoff §7 ban): c366bf4716d3b049d95411dd9301043e447bdaa4
Workspace (outside repo): C:\Users\moilo\AppData\Local\Temp\opencode\bcon-wab\plan-a-*
Spec authority: C:\pico-bcon\docs\superpowers\specs\2026-09-15-uart-features-design.md + C:\pico-bcon\spec\protocol_v3.md (SSOT until Task 9 revises it).

## Rulings
- Ruling: no worktree (tree holds uncommitted Phase-3 work, no consent for isolation ops); sequential dispatch only, one implementer at a time. Cost if wrong: task interference; mitigated by strict file scoping per task.
- Ruling: NO commits (handoff §7 ban until explicit approval). Implementers leave working-tree changes uncommitted; reviews use `git diff` scoped to the task's files + `git status`. Ledger records file states, not commit hashes. Cost if wrong: none for correctness.
- Ruling: single available subagent tier ("general"); Task tool has no model parameter, so no model selection is possible in this harness. Cost if wrong: none (uniform tier).
- Ruling: workspace = existing Temp bcon-wab dir (plan-a-task-N-report.md, plan-a-progress.md here), NOT .superpowers/sdd/ (not git-ignored; Temp precedent from W-series). Cost if wrong: stray ignored-dir noise; mitigated by Temp use.
- Ruling: no task-brief script on win32 (scripts are .sh); the plan file's Task-N section IS the brief — dispatch prompts point at plan path + task number. Cost if wrong: none (single source, no duplication drift).
- Ruling: host suite must run from a vcvars64-initialized cmd with full-path cmake/ctest (AGENTS.md). If the implementer lacks it, it reports the step as not-run concern instead of blocking the task. Cost if wrong: host evidence lands late; mitigated by reviewer scrutiny + controller re-run.

## Pre-flight conflict scan
| Pair / Task | Produces vs consumes | Finding |
|---|---|---|
| T1 vs T3 (test_config.c) | T1: vectors [0..2]; T3: appends new block, distinct region | Clean (sequential anyway) |
| T1 vs T5 (dispatch.c) | T1: HELLO branch; T5: helper + fields, distinct regions | Clean |
| T1 vs T7 (protocol.h, dispatch.c) | T1: PROTO_VER line + HELLO; T7: enum append + STATUS_REQ, distinct | Clean; order T1→T7 enforced |
| T3 vs T4 (rumble.h) | T3 creates + decl; T4 includes + calls | Clean; order T3→T4 enforced |
| T3 vs T5 | No shared prod files (test_config appends, distinct blocks) | Clean |
| T4 vs T6 (hid globals) | T4 produces probe_rumble_l/r; T6 consumes in feed | Clean; order enforced |
| T5 vs T6 (dispatch helper) | T5 produces v3_rumble_tick + ACT; T6 consumes | Clean; order enforced |
| T5 vs T7 (dispatch.h enum) | Both append; T7 runs after T5 | Clean (sequential) |
| T6 vs T8 (main.c) | T6 rumble feed/flush; T8 player feed/flush, adjacent regions | Clean (sequential) |
| T4 vs T8 (hid.c/h) | T4 rumble branch; T8 0x30 seen-flag, distinct regions | Clean |
| T7 vs T8 (dispatch helper) | T7 produces v3_player_tick + ACT; T8 consumes | Clean; order enforced |
| T9 vs all | Spec file only | Clean |
| Rubric vs plan | Template "commit" steps already replaced by status-verify in plan text | No conflict |
| T2 self-check | Spike deliverable = mapping + vectors; no code | Clean; T3 blocked until T2 reports (non-BLOCKED) |

## Progress
Task 1: complete (review clean; 2 deferred minors: protocol.h:1 PROTO_VER=3 comment + dispatch.c:9 DOWNGRADED comment ? both outside brief sites, owned by Task 9; reviewer-flagged attribution caveats on untracked-file baselines noted, end-states verified)
Task 2: complete-as-spike (layout + neutral log-proven + reference formula + R1-R3 scaffolding; 762x A2-10 all neutral, counter byte cycles 00-0F, off-by-one catch for Task 4 confirmed in-repo). Real-vector gate (?3 incl. non-zero) NOT met ? no non-zero rumble in any log.
Ruling: rumble chain Tasks 3-6 PARKED pending one HW vibration capture (spec ��6.1 forbids grounding the formula without real-log bytes; brief STOP gate aligns with spec, so no waiver ? cost if wrong: days of desk progress lost, but HW batch already exists so marginal cost is one capture; implementing on reference-only scaling risks shipping wrong telemetry gains). HW checklist += 'vibration test capture with hci_dump'.
Ruling: Task 4 corrected per spike ��4 (the correction the spike was designed to produce): decode &report[1] with report_size>=9 guard; report[0] is the packet counter. Task 3 signature unchanged (takes rumble8). Cost if wrong: L-shifted amps + R truncation ? caught by V0 test + HW confirm.
Ruling: order swapped ? Tasks 7+8 (PLAYER_INFO, fully evidenced, independent) proceed now; Task 9 (spec) runs after the rumble chain completes so spec text matches code.
Note for unparked Task 5: append ACT_SEND_RUMBLE at enum END (after ACT_SEND_PLAYER_INFO) ? never insert before it (implementer-flagged renumbering hazard).
Task 7: complete (review clean; 1 deferred minor: ob-full drop marks sent_* (dispatch.c:110-113) ? best-effort telemetry edge, final review triages).
Ruling: Task 7 finding amends unexecuted Task 5 (same shape) ? v3_rumble_tick sketch now updates sent_* only when ob_n advances (retry-next-tick on full outbox). Cost if wrong: re-review noise on Task 5; reviewer will verify the guard. Task 7's own instance stays as-is per reviewer (no plan amendment without loop); final fix wave may align both.
Task 8: fix round 1/5 (1 addressed, 0 open ? probe_player_seen reset in probe_hid_reset; wireless exit 0 fresh dir). Ruling: Task 6 unpark amendment ? RUMBLE flush arm goes immediately after the PONG arm, before the PLAYER_INFO arm (order among ACT arms is cosmetic but fixed for reviewability).
Task 8: complete (review clean after fix round 1/5; fix verified at hid.c:106, wireless exit 0, placement verbatim).
Adjudication (controller, review Important ? host not re-run after one-line hid.c fix): tests/host/CMakeLists.txt compiles test_config from test_config.c + protocol.c + dispatch.c ONLY (hid.c absent, verified by grep) ? the fix cannot affect host evidence; carried 3/3 stands, no re-run required. Ledger-only resolution, no new round.
Plan A desk-state: DONE = T1, T2-spike, T7, T8(+fix). PARKED(HW) = T3-T6 (need one non-zero A2-10 capture). DEFERRED = T9 spec (after rumble chain, so text matches code), final whole-branch review (branch incomplete by design). Rumble-chain unpark order: HW capture �� T3 �� T4 �� T5 �� T6 �� T9 �� final review.
W8 HW verdict (log/COM3_2026_09_15.21.09.02.303.txt, 6 boots, owner run 21:09-21:36): U5 CLOSED ? every timer in every boot is exactly +1 (usb/empty/stats cumulative diffs +1..+5 track boot count perfectly); W7 +2 was two-boot accumulation, definitive. U3 CLOSED-for-mechanism ? del=0/0 in all 5 death boots + 1 survival boot; delete never involved; W7 del=1/1 was foreign accumulation. W8-IMPL BUG: boot-delta lines always 0/0 by construction (baseline snapshotted in diag_rec_init BEFORE w8_boot_dump runs, so current==base at dump) ? totals+epochs carried the verdicts instead; no W8b needed (post-hoc differencing suffices). NEW two-signature model: epochs 1-4 died INSIDE the op (safe/tag begins>ends, no write-log line, host stays 0; last app event = PSM-0x11 CHANNEL_OPENED, hid-open handler entered store first thing, timers dead, WDT +2s); epoch 5 completed (write-log+host saved, host=1) then died ~2s later (classic Death-B). Epoch 6 (bonded, BTL key-write, BCHO dedupe-skip) survived 26+ min with 26 SUBs; SNIFF mode 0->2 transitions survived. Killer correlates with BCHO/host-path writes (or their link phase), NOT any-flash-write: pre-L2CAP key-write survived. Vibration: zero non-zero A2-10 in 12MB ? stays deferred per owner (needs STATE-driven UI navigation; poc_send.py CAN send STATE today, so path exists once a surviving session is held). Color/PLAYER_INFO: not run (zero hits).
External review #3 (opdeath brief) received: op�����F�� OK; top logic check ? tag BEGIN inside callback means outer lockout was ACQUIRED, so H1-outer contradicts counters; prime suspect = DOUBLE flash_safe_execute (outer wrapper + SDK HAL inner). Controller VERIFIED from SDK 2.3.0 sources: inner safe-execute real (pico_btstack/btstack_flash_bank.c:82 erase, :170 page-program); nested lockout protocol-transparent by code reading (victim re-locks on new request id: multicore.c handler spin vs update_lockout_request_id) ? nesting per se does not deadlock; hang needs a stage-specific trigger. Bank geometry: TOTAL=2 sectors (4KB/bank); 'at 89d' = 89 decimal bytes into 4KB bank ? simple capacity-migrate implausible for ep5. W9 brief written (Temp briefs/task-11-brief.md): 9a our-layer stages S0-S3/S10-S12 (S4-S9 excluded: SDK boundary, documented), 9b wrapper-removal A/B with granularity disclosure, 9c bank-CRC-per-boot via XIP reads. NO-GO: abort 9b if build SDK lacks inner safe-execute.
External review #4 ACCEPTED (strategy): 9c leads as branch parent (CRC-change��H2/migrate+W11; CRC-unchanged��pre-write-scan infinite loop becomes prime, worker gains bank-integrity check + iteration cap + reformat path); 9b after localization as transparency falsification; W10 repositioned to Death-B-only track; worker provisional-proceed (hemostasis) with final scope gated on 9c; timeout never unconditional (wrapper-first ordering; SDK 2.3.0 lockout-timeout fix inclusion must be commit-verified before any finite timeout). Reverse-question ANSWERED from desk: per-boot tag/tombstone inventory NOT in evidence ? zero 'Found tag'/'Erase tag'/'migrate'/bank lines in the entire 12MB W8 log (only the two write lines :1145/:1354); boot-time TLV reads predate hci_dump/log init so they are invisible; link keys=0 all 6 boots. No desk pre-read possible ? 9c must be burned. Task-11 brief reordered accordingly (Phase1 = 9a+9c combined observe UF2).
Owner order: burn W9-observe FIRST (9c is branch parent; worker permanent part stays [PENDING 9c]; hemostasis-only worker pass may run in parallel). Brief amended with 3 binding pre-burn checks (snapshot-before-TLV-init/loads, per-bank 4-point set, single epoch-linked dump). UF2 target: log/pico-bcon-w9-observe.uf2.
W9-observe BUILT (Phase-1 only): log/pico-bcon-w9-observe.uf2 827392B SHA 1BA21756, exit 0 (one self-caught brace fix, disclosed+reverified); 3 pre-burn checks PASS (snapshot L1003 pre-TLV-init/loads; current_bank/write_offset via public header + snap/dump/prev; single epoch-linked block); restore hash-proven + markers zero (controller-verified, W8 precedent). Concerns C1-C8 documented (C2: S0=0 vs empty disambiguated via opstage_n; C5: compare consecutive WDT boots, not virgin boot). Awaiting owner flash run.
W9-observe HW run #1 (log/COM3_2026_09_15.23.29.33.344.txt, 2 boots, prelude: keys deleted + Switch deregistered + Grip-screen connects): NO op-in-death reproduced. Epoch-1 death = Death-B fully staged: S0-S12 complete trail (w9opstage last=S12 n=7), single clean program (wroff 2255->2269 +14B, active 0, no migrate), dur=10793, host=1 persisted, then ~2s scheduler death. chg0=1/chg1=0 = the committed write itself (expected, not branch evidence). Epoch-2: BTL key-write completed (offset 8dd=2269, contiguous) + BCHO dedupe-skip (hid-open print, no write line) -> TWO full SUB runs + long survival; SNIFF 0/2 flaps survived; Switch 0x13 + operator-driven reconnects incl. one hid-open FAIL 0x0e (benign). Refined model: Death-B follows BCHO-via-wrapper completion; BTL-via-HAL completion (twice now: W8ep6, W9ep2) survives ? discriminators remaining are (a) wrapper/double-wrap post-return damage [9b] vs (b) link-phase at write time [phase-gated]. Op-in signature (W8ep1-4) still needs 9c-on-in-op-death; bank preconditions for op-in unknown (W8 bank wroff~89 vs this run 2255).
External review #5 ACCEPTED with order correction: op-in reproduction window is closing, so Phase 0 (zero-code-change reproduction on the still-flashed observe UF2) precedes 9b (9b mutates flash and is burnable any time). W8-prelude forensics from desk: W8 boot1 host=0 + link keys=0 + young-bank offsets (86b->89d lineage) with NO wipe/delete lines in any log ? clearing event unrecorded; operator asked directly (full-chip-erase? board swap? KEY_DELETE?). Correlation noted: op-in 4/4 on young bank, 0/N on mature banks (W1/W5/W7 completions + W9 run). 9b brief amended with outer-return-to-2s breadcrumbs (timer/async last-service, PRIMASK, list head iff public accessor).
W8-prelude SOLVED by owner testimony: T_KEY_DELETE frame (TYPE 0x33/LEN 0/SEQ 0 + CRC8) sent over data UART at 115200 -> FX_KEY_DELETE (tombstones, wroff unchanged) -> USB power unplug/replug (NOLOAD cleared) -> Switch untouched. Virgin-bank theory DEAD (no erase ever); tombstone-scan side confirmed as recreation target AND top desk suspect for the S2/S3 hang (pre-write iterator walk over tombstoned entries). Phase-0 protocol rewritten to the exact recipe (verify 'keys deleted' line before power-cycling).
Phase-0 reproduction result: NO death reproduced ? instead a hid-open with NO flash write at all. Tombstone-bank boot (host=0/keys=0, epoch fresh) idled 170s, Switch-initiated incoming conn -> non-bonded pairing -> encrypt -> BOTH PSMs -> hid-open printed 'saved' but with NO 'write BCHO' line and NO 'host saved (n=1)' -> full SUB run kinds 1-9 -> session alive (MODE_CHANGE flaps + neutral rumbles survived). BCHO write expected (host=0 defeats dedupe) but absent. Ranked explanations: (i) flash_safe_execute refusal inside store (would show safe=1/0,tag=0/0 at next dump); (ii) unseen earlier session in the t=12-170s pasted gap that stored first (this open deduped); frames=3 constant rules out UART FX. MISSING DATA: middle-log portion + next-boot dump totals. Tombstone-bank + incoming-conn recipe yields skip-or-refusal, not op-in.
Phase-0 VERDICT (file-authoritative log/COM3_2026_09_15.23.50.44.146.txt, 3 boots): OP-IN REPRODUCED on tombstone bank. Boot1 (tombstone boot, host=0): incoming conn -> non-bonded pairing -> encrypt -> BOTH PSM accepts -> 0x11 CHANNEL_OPENED (dev 02:54.972) -> ERTM -> SILENCE (no SUBs, no 0x13 open) -> WDT. Dump: safe=1/0 tag=1/0, S-trail 0,0,0,0,0,1,2,3 (n=4, exactly one S0-S3 run), chg0=0/chg1=0, wroff 2305 unchanged, host=0. Boot2: same bank, incoming conn -> open -> store COMPLETES (write at 901, S12 trail, host=1) -> Death-B ~2s later. Boot3: OUTGOING reconnect (reconnect_timer works) -> bonded key-save at 90f -> host dedupe-skip -> SUB full run -> SURVIVES (file ends alive). BRANCH DECISION (b): ZERO mutation across in-op death -> pre-write-scan CPU-side loop over tombstoned bank is PRIME suspect (H2-migrate demoted for op-in: no erase could have run). Worker amendments CONFIRMED-pending: boot-time bank-integrity check + scan iteration cap (+ safe reformat path). Note: same-bank first-store hangs, second-store completes ? 'first touch after tombstoning' pattern; timing/phase alternative kept as second. 9b (wrapper A/B) still required for Death-B wrapper-vs-phase split.
Worker doc: permanent additions LANDED (Temp design-quiesced-worker.md -> archived plans/quiesced-worker-design.md): ��4.9 bank-integrity check + TLV_SCAN_TRIP_CAP=1024 scan cap + reformat path; ��4.10 timeout ordering (9b-first, SDK commit gate); quiesce-alone-sufficiency claims qualified everywhere; scenarios 12-14 + acceptance bullets. 9b BUILT: w9a-control + w9b-nowrap UF2s (828416B each, exit 0 after 1 self-caught missing-decl fix); single-variable proof = wrapper hunk + 1 label line (w9ab-a-vs-b-store.diff); diag_rec identical both variants; safety GO (inner verified :82/:170); concerns: S1/S12 semantics shift in B (pre/post-direct-call ? read B dumps with shifted table), D-gate double wear, virgin-boot magic mismatch (000B vs 0009). Restore hash-proven + markers zero (controller-verified). Awaiting owner 9b A/B run.
9b A/B HW (owner pastes 2026-09-16 ~01:30-31): BOTH ARMS DIE ? outer wrapper exonerated as hang site. A (double-wrap): forced op hangs post-Erase-log with S10/ENDs missing (hang between internal delete-old logging and S10 = subsequent inner-lock re-acquires or store epilogue); S3-hangs also seen. B (no outer): boot1 = normal store COMPLETES (write a69+host saved+Erase) then forced op hangs at S3 (trail 3,10,11,12,0,1,2,3, n=11, safe=2/1) -> death. Unified: EVERY hang sits at an INNER-HAL lockout acquisition (first: S3-hangs incl. W8ep1-4; Nth: post-Erase-log hangs). Never at outer-acquire; outer-acquire has zero hang evidence in any run. Death-B-without-wrapper STILL OPEN: B-boot2 completed a write (aa9+Erase 2679) but the pasted B log ends mid-session (~01:30:36) before its fate ? NEED file tail. ANOMALY: a 'post=3 current=4 safe=4/0' chunk with backward timestamp (01:30:38) in the A paste; provenance unclear (possible separate pilot run); EXCLUDED from verdicts pending operator clarification. Comparability caveat: A ran after B on same chip (bank evolved between arms); single-variable ideal would interleave ? noted, does not affect 'wrapper not necessary' conclusion.
9b A/B HW verdicts (logs 01:30 B + 01:31 A): (1) OP-IN WITHOUT WRAPPER CONFIRMED ? B-boot1: normal open store COMPLETED first (S0-S12, write a69 + host saved + Erase 2665), then forced gen=1 HUNG at S3 (trail 3,10,11,12,0,1,2,3 n=11, safe=2/1) -> death. S3-hang with zero outer involvement (B has no outer; S3 precedes first inner op) = hang in scan/migrate-decision reads or first inner-lock acquire. Outer-acquire excluded empirically both ways (W8 S3-hangs had outer present-but-passed; B S3-hang has no outer at all). (2) A-arm: normal skipped (host=1 dedupe), forced reached write+Erase logs (ae1/2771) then died with S10/ENDs missing = hang in LATER inner-lock re-acquires (zero-page programs) or store epilogue. (3) H2-MIGRATE DEAD for all observed deaths: offsets march contiguously (a69->a85 key->aa9->ae1->b21..., active 0, no migrate line anywhere). Unified: every hang sits at an inner-HAL lockout acquisition (first or Nth); completions prove the same acquisitions usually succeed -> intermittent victim-side race is prime. NEW bounded desk lead (no HW): audit Core1 IRQ-disabled windows (mutex critical sections) vs inner-handshake instants. OPEN: B-boot2 fate past 01:30:36 (completed write aa9+Erase 2679, session alive at paste end ? Death-B-without-wrapper iff it died ~2s later); A-boot2 fate on clean data (foreign 01:30:38 post=3 chunk stays EXCLUDED).
CORRECTION (controller code re-read, SDK flash_bank.c:439-451): the S3-to-write-log window contains ONLY arithmetic + two not-taken branches (migrate NOT entered: no migrate/erase logs anywhere; error-return NOT taken: no log_error line, ends would be 1 with rc=2) + the write log_info itself. NO loop, NO lock, NO I/O in path. A CPU-loop there is IMPOSSIBLE in straight-line code under both IRQ states (A-arm holds outer with IRQs disabled; B-arm has none) with zero FAULT lines (reporter tested working). Therefore the scan-infinite-loop candidate has a HOLE: proponents must name the loop or concede. S3-hang micro-location is REOPENED with surviving candidates: (i) wedged higher-IRQ at that instant (B-arm only; cannot cover A-arm with IRQs off), (ii) instruction-fetch/XIP stall (covers both IRQ states, no fault, timers dead, WDT ? NEW prime suspect for S3-hangs; desk-checkable via link-map placement of the store_tag path + QSPI stall conditions), (iii) silent exception (reporter re-validation needed to fully close). STANDS UNCHANGED: wrapper irrelevance (both arms all flavors), H2-migrate dead (offsets/active/logs), Death-B post-return damage, BCHO-path specificity, U5 +1/boot, U3 del=0, bank-state correlation for op-in (young/tombstone). NEXT DESK (no HW): link-map placement audit of tag_store_fn/store_tag/migrate path (XIP vs RAM) + get_size indirection check + fault-reporter re-validation procedure.

Task 12 (W10) RULINGS — accepted localizations + diagnostics (report: sdd/plan-a-task-12-report.md; artifacts in logs/ + diffs/w10-*):
- Ruling ACCEPTED (op-in death localization): the failing store is the one that must erase an old record; the hang sits in `delete_tag_until_offset`'s zero-fill program (an additional `flash_safe_execute` inside the same op) waiting for the lockout ack. Evidence: 9b A/B both arms die in the exact "write → Erase tag → silence" shape; erase-free stores complete (A-boot1's first store needed an erase and died; B-boot1's first store needed none and completed, the second died). Excluded items stand (migrate/H2, XIP-resident code in the flash path, mutex IRQ-disabled windows, bounded iterator, SDK-version fix). Cost if wrong: recovered by the W10a run (it re-measures the same boundary).
- Ruling: Death-B (W9-observe ep1: no erase, S12 complete, ~2 s death) is NOT explained by this mechanism — keep it OPEN and SEPARATE (no merging of the two signatures). Common trigger = BCHO store during connect, so the quiesced worker heals both. Cost if wrong: worker/walker design premises (bleed-stopping scope) would need revising.
- Ruling (rec#1): 4-point breadcrumb (lockout req-before / ack-after / ROM-before / ROM-after) implemented via linker `--wrap` (zero SDK/BTstack source edits; revert = w10_diag.c/.h + w10diag.cmake + the marked hunks in main.c/store.c/store.h/CMakeLists). Prior: the immediately-preceding lockout succeeding while N+1 stops argues for (b) ROM stall or a consecutive-`flash_safe_execute` async_context/re-arm race over pure (a) victim non-response. Cost if wrong: none (the run settles it).
- Ruling (rec#2): the 100 ms timeout is a DIAGNOSTIC (W10b) — observe recovery vs re-hang. NOT a permanent fix; the HAL instrumentation is diagnostic-only and MUST be reverted, and any permanent finite timeout still needs the ledger-line-48 SDK commit gate.
- Ruling (rec#3): the normal-build run is authorized; frame it as "rare but not harmless — the erase-path mine remains" (BCHO for a different peer / BCCL color / BCW1 capture / BCWR wired saves that must zero an old record; first traversal or console-key rotation still dies). Production survival today = dedupe-only (no write) sessions.
- Desk-audit partial closure (this session, feeding line-59 NEXT DESK): .map shows flash_range_program/erase and multicore_lockout_handler are RAM (`.time_critical`) and flash_safe_execute/lockout handshake are XIP (called with XIP on) → the XIP-stall candidate (ii) is rejected as "code executing during the flash op"; pico-sync mutex waits restore IRQs (`spin_unlock` = irq-restore, lock_core.h:208) → the Core1 IRQ-disabled-window lead is rejected. get_size indirection + fault-reporter re-check remain not-done; the (a)/(b) fork is delegated to W10a.
- Task 12 status: BUILT + RESTORED. log/pico-bcon-w10a-crumb.uf2 SHA C59C2EE4…A5E7, log/pico-bcon-w10b-100ms.uf2 SHA 5BB9F686…763A, log/pico-bcon-prod-wireless.uf2 SHA C6C35188…7ACD; restore hash-proven (main.c F40AFE1A…, store.c 40456D82…, store.h FF4B5284…, CMakeLists 6E3628C3…), status 30 lines identical, markers zero. Awaiting owner HW run order: W10a (rec#1) → W10b (rec#2) → optional prod (rec#3).
- W10a HW VERDICT (owner run 2026-09-16 ~02:38, bonded reconnect, link keys=1): NO op-in death; Death-B 6/6 with the flash op FULLY COMPLETE every epoch. Evidence: per-epoch lk req=ack (11/11, 15/15, 19/19, 23/23, 27/27; fail=0), rom p beg=end (9/9…21/21), outer FSE_OK 3837–3859 µs as the last HAL event; forced store = exactly 3 page programs (value/header/erase-zero, ~485–491 µs each); `host saved (n=1)` in log = the FORCED store's own print (store_host_inner prints for force too; the normal store deduped silently), then `w10fire var=D gen=N`, then ~2.0 s of silence and a WDT reset (`wdt=1 host=1`). So: (a) Core1 non-response NOT observed (every ack arrived); (b) ROM stall NOT observed (every program returned). The store-completes-then-dies shape CONFIRMS the separation (Death-B mechanism is not inside the lockout/ROM path). Reading: the erase-path store is not inherently fatal — the bank was mature (writes in the 0x3fdd00 page; write_offset advanced far from the young-bank offsets), so the op-in precondition (young/tombstone bank per ledger line 8/55) was not met. Corollary: the D-gate's "BCHO double-write + erase path" is now a DETERMINISTIC Death-B trigger in this state. Ruling: W10b (rec#2) stays ON HOLD — the 100 ms cap can only fire on an unacked lockout, which never occurred; it would just repeat Death-B. rec#1's (a)/(b) still needs the op-in run: W10a + KEY_DELETE(0x33) → power cycle → fresh Change-Grip pairing (i.e. the 9b/Phase-0 prelude that hung before).
- Task 13 (W10c) BUILT + RESTORED: W10a verbatim + Death-B 2s-window crumbs (last timer service per owned timer, post-poll 1ms loop sample with PUBLIC timer-list walk (cap 16, read-only) + PRIMASK/BASEPRI, 5-stage poll trail, HCI TX/RX liveness; hci_dump stays OFF; reconnect timer not instrumented). Recorder VER bumped to 2 (cross-flash resets). log/pico-bcon-w10c-deathb.uf2 824832 B SHA 61F303A4…05D. Report: sdd/plan-a-task-13-report.md (includes owner runbook + Death-B decision table). Restore hash-proven + status 30 + markers zero (W10DIAG for the HAL/D-gate set, W10C for the loop set). Run = same as W10a (any reconnect triggers the D-gate); read `w10poll`/`w10tmrsvc`/`w10loop`/`w10hci` alongside the HAL close and report the single number `w10loop us` − (last FSE_OK ts).
- Task 14 (Death-B fix) IMPLEMENTED, UNVERIFIED (report: sdd/plan-a-task-14-report.md). Condition (1) proven by grep: SDK flash.c:106 `static uint32_t irq_state[NUM_CORES]` (shared, not local) + :203 enter-store + :210 exit-restore → nested safe_execute clobbers the slot, outer exit restores PRIMASK=1. Fix = drop the outer wrapper (tag_store_safe + both forget paths → direct calls; HAL inner coverage per 9b §1; Core1 is parked per-mutation by the inner calls, and Core1 never writes flash) + permanent post-store PRIMASK assert/self-heal (`store_irq_guard`: entry==0 && exit!=0 → log `irq leak healed (n=…)` + `cpsie i`) + permanent BCON `pm=` field. log/pico-bcon-fix-verify.uf2 SHA 1B179E49…57CE (fix + TEMP D-gate, reverted after build; FIXVERIFY grep zero) and log/pico-bcon-prod-fix1.uf2 SHA 2DCD81B6…65DF (fix only = current tree). Owner verify: flash fix-verify, connect (D-gate fires per HID-open), expect NO Death-B + pm=0 + `irq leak healed` never appears. Ledger notes: the OLD prod UF2 keeps the mine (every nested store leaks; BCON pm is the detector); op-in-after-outer-removal is a SEPARATE re-verification (open; needs young-bank prelude + crumbs build); W10b unnecessary confirmed (bug is on the success path); commit HOLD until HW pass. Wakecon answer (read-only check): its store.c calls tlv->store_tag/delete_tag DIRECTLY (no outer wrapper) and it shows no Core1 use — single-level only, hence never nested, hence never leaked.
- Task 14 VERIFY PASS (owner run 2026-09-16 ~09:18, fix-verify UF2, fresh pairing host=0/keys=0): normal store `host saved (n=1)` + forced store `host saved (n=2)` + `forced store done` (both returned) → FULL SUB handshake kinds 1–9 → session alive 20 s+ (BCON t=16–20 s, iters advancing, rum counting). `pm=0` in EVERY BCON line (pre- and post-store) and NO `irq leak healed` line → the leak is gone by construction and the guard never fired. Death-B ELIMINATED on the exact trigger that killed 6/6 before (D-gate double-write + erase path). No commit made (AGENTS.md: explicit request required; owner hold now liftable at their discretion). Remaining open: op-in-after-outer-removal re-verification (young-bank prelude + crumbs build); W10b unnecessary stands; prod-fix1 UF2 is the flash candidate for normal use.
- Task 15 (op-in re-verify) BUILT, AWAITING OWNER RUN (report: sdd/plan-a-task-15-report.md): fixed tree + W10c crumbs verbatim (VER 3, fresh epochs) + --wrap, NO D-gate (the original op-in cases were all natural first stores, so the honest trigger is the prelude, not a forced write). log/pico-bcon-opin-verify.uf2 822272 B SHA CFFB457D…DFAE (wireless/115200). TEMP reverted, fix kept (CMakeLists/main.c byte-identical to fixed baseline; markers zero; prod-fix1 UF2 rebuilt from final tree and byte-identical to stored copy). Owner prelude (same as 9b/Phase-0): KEY_DELETE frame [AB][33][00][SEQ][CRC8] over data UART → `keys deleted` line → USB power cycle → fresh Change-Grip pairing → capture 2–3 boots (no power cycle between death and capture). Verdict paths: req>ack (or fail>0) with last event at LK_REQ = op-in persists → (a)/(b) split, needs its own fix (do NOT re-add the outer wrapper); all-closed + survival + pm=0 = op-in gone for this bank state (repeat prelude 2–3×; single survival does not prove absence). poc_send.py gained
--key-delete (sends the 3 prelude frames; use it instead of hand-rolled snippets).
- Task 15 VERDICT (owner run 2026-09-16 22:41, opin-verify.uf2 = fixed tree + VER3 crumbs, no D-gate; prelude done: host=0/keys=0 at boot): NO op-in death. Natural first store on tombstone bank COMPLETED (`host saved (n=1)` = returned) → FULL SUB handshake kinds 1–9 → 90 s healthy survival (BCON t=31–120 s, cid=1, neutral-rumble intake 0+4→0+736, iters advancing, `pm=0` on every line, no `irq leak healed`, no WDT). Together with the 09:18 fix-verify run (fresh pairing: normal + forced/erase-path stores both completed → SUB complete → 20 s+ alive), BOTH op-in flavors are covered on the fixed tree: Phase-0 flavor (pre-write S3 hang) is eliminated by construction (no outer lockout exists); 9b-B flavor (post-erase hang) did not fire in the targeted erase-path trial. Ruling: op-in CLOSED (no remaining mechanism + negative trials in both flavors; intermittent-race caveat stands, crumbs builds archived for any recurrence). Death-B CLOSED (verified). Both WDT signatures resolved.
- Task 15 second trial (owner run 2026-09-17 01:51, opin-verify.uf2, prelude done host=0/keys=0): NO op-in again. Natural first store COMPLETED (`host saved (n=1)` once, no D-gate in build) → FULL SUB kinds 1–9 → 70 s healthy session (BCON t=18–70 s, cid=1, rumble intake 0+0→0+430, `pm=0` every line, no guard fire, no WDT). Op-in stays CLOSED (2nd independent negative trial on tombstone bank).
- Task 16 (UART baud hunt) SPEC DONE, implementation pending (spec: docs/superpowers/specs/2026-09-16-uart-baud-hunt-design.md). Positioned as Sub-project B-0: discovery layer UNDER Plan-A spec §3 BAUD_SET (negotiated switch), not a replacement — B handles deliberate post-lock switches, B-0 handles initial/unknown/adapters/order-free discovery on the shared rate table. Ruling: 3-layer design (L0 persisted last-good as sweep slot 0 + L1 gated brute-force sweep with 2-frame lock + L2 exact-negotiation deferred); hunt gated on (never-locked AND no BT session), frozen after lock; PC contract minimal (open once, blast PING till PONG); no PIO measurement, no unconditional re-hunt, no mid-session tracking. Cost if wrong: none for correctness (desk-only spec; implementation will carry its own verify build). Precedents recorded in spec (Zephyr HL78xx try-list, NXP ping, Wikipedia ABR, Bus Pirate PIO discussion). Next: owner go → implement (Core1 hunt machine + TX gate + poc_send hunt mode + verify builds) as task-16 impl.
- Task 17 (PokeCon compat) SPEC DONE, implementation pending (spec: docs/superpowers/specs/2026-09-16-pokecon-compat-design.md). Wire dialect verified from two primaries (KawaSwitch wiki serial-contents page + yqYo1 ParseLine impl): 115200 8N1, space-separated hex line `<btn16> <hat> [<lx> <ly> [<rx> <ry>]]\r\n` + words (`end` etc.); button table LSB-first [RS,LS,Y,B,A,X,L,R,ZL,ZR,MINUS,PLUS,LCLICK,RCLICK,HOME,CAPTURE] with VIIPER map fixed; hat 0-7+8=center → dpad buttons; sticks 0-255/0x80-center/Y-down-positive with LS/RS routing quirk; wiki example `92 8 80 ff` stick bytes flagged as prose typo (table+parser rule). Ruling: boot-time input mode (build flag v1; v3 default untouched; no auto-detect), feed into g_s.state so BT/USB/pack/timeout all reuse; keep 200ms timeout-neutral in pokecon mode (safety; lengthen-not-remove if scripts gap); TX quieting optional; MCU/keyboard/Date words out of scope. Next: separate session reads repo + spec, implements (host mapping vectors → FW build → HW verify per spec §5).
- Design freeze (desk, 2026-09-16 PM): task-15 procedure re-verified solid as-is (prelude + verdict paths need no change); task-16 appended with implementation notes (session signal via g_s bool under g_m, pre-launch BCBR load, non-blocking sweep mechanics, TX gate, lock line, TLV save via inbox FX; host tests none); task-17 appended with src/pokecon.c pure-module placement (host-testable like pack.c); NEW Joy-Con annex (docs/superpowers/specs/2026-09-16-joycon-personality-design.md: table schema, ProCon verbatim row, L/R TBD capture list, MAC/TLV design, PC SL/SR open point, PERSONALITY_SET flow, test plan); NEW rumble capture procedure (docs/superpowers/specs/2026-09-16-rumble-capture-design.md: Find-Controllers method + hci_dump 2-line Temp + A2-10 extraction + vector format + unpark order; formula stays TBD per spec §6.1).
- Decision (STATE-12, owner-approved 2026-09-16): sticks go 12-bit via LEN-dispatched STATE (LEN 8 = legacy u8x4 center 0x80 / LEN 12 = u16LEx4 center 0x0800, same Y inversion in FW, BT/HID untouched). Fits the v4 migration; needs spec note + parser LEN accept + dispatch branch + host vectors + poc_send support. Queued behind rumble-chain HW (no HW needed for the protocol part).
- STATE-12 IMPLEMENTED (TDD: RED watched at dispatch-assert + LNK2019 + pack-truncation, then GREEN 3/3 host + FW build exit 0). Scope: proto_state_len_ok() shared by parser+dispatch; ctrl_state_t/usb_sub_ctx_t/probe_*/bcon_* widened u8->u16 (centers 0x800); pack_stick_12bit takes u16 (same inversion/clamp); Core1 ingest branches LEN 8 (<<4) / LEN 12 (u16LE &0xFFF); NEUTRAL/selftest/timeout/boot centers 0x800. spec §5.1 + poc_send --hold12/build_state12 added. log/pico-bcon-state12.uf2 built (wireless/115200). Uncommitted. Awaiting HW verify: --hold12 extremes on the Switch input-test screen (or USB 0x30 reports).
- STATE-12 HW VERDICT (owner run 2026-09-16, state12.uf2): PASS with two notes. Full deflection OK, intermediate values confirmed. Y convention fixed by observation: LY 0x000 = up, LY large (0xFF0 class) = down (Y-down-positive, matches wiki/PokeCon convention). Smoothness NOT yet verified (needs continuous motion; poc_send.py gained --sweep12 left-stick circle for it). No log cross-check possible in that run (single UART adapter on data side); screen observation sufficed. Single-adapter procedure stands.
- STATE-12 COMPLETE (owner HW 2026-09-16): smooth circular motion confirmed via --sweep12 → true 12-bit path end-to-end. Feature done (spec + parser/dispatch + pack + poc_send + host green + FW build + HW pass).
- Task 16 HW RUN (owner-assisted, agent-driven serial 2026-09-17, baudhunt UF2s): A PASS (115200 single-slot: ping 3/3 + HELLO_ACK ver=4 result=0 + STATUS 1Hz flags=0x27). B1 PASS (1M: ping 4/5 with 1st TIMEOUT = pre-lock gate, exact predicted signature). B2 PASS (HELLO_ACK + STATUS @1M, flags=0xC6 = live Switch session). B3 PASS (115200: 0/2 TIMEOUTs = no false lock; back to 1M: 3/3 = lock held, no re-hunt). B5 BLOCKED-then-ROOT-CAUSED: post-reboot 115200 blast (5000 STATEs) + 1M blast (8000 STATEs) both 0/3. LOG-side capture (owner moved adapter to GP0/1) showed BCON t=31-77 with cid=2 + rum=0+10 (Switch auto-reconnected via TLV keys) + baud=0H FROZEN (session gate parked sweep) + monotonic uptime + pm=0 + advancing iters (healthy, no resets). Reconstruction: post-boot Switch reconnect parked sweep at a middle slot (50%) so both blasts were line-rate mismatches; B1 passed by park-at-1M luck (25%). Ruling: the (!locked && !session) gate strands the PC whenever the Switch connects first (always: reconnect lands seconds after boot, PC attaches later) — gate REMOVED (!locked sweeps regardless of session; pre-lock UART carries no session traffic so harmless; freeze-after-lock kept). Ruling (PokeCon variable-baud correction): PokeCon app baud is VARIABLE (owner correction; task-17 spec §1.1 115200-fixed premise flagged for revision) — in-band byte sequences cannot cross rates (framing fails), so re-hunt signal is out-of-band BREAK (RX-low >=20ms; normal traffic max low ~78us@115200; gpio_get poll, UART-independent, any baud) → unlock + sweep restart (slot0=current). PokeCon connect ritual = send_break(~100ms) → set baud → stream; poc_send gained --break_. Auto-unlock variants REJECTED (silence-timeout indistinguishable from idle; session-down unlock never fires in PokeCon use since BT stays up; DTR/BREAK-send absent; CRC-error-rate flaps). In-band dedicated sequence also REJECTED (undecodable across rates). Lock semantics now: locked until reboot OR BREAK. External probing proposal (115200 rendezvous → high-to-low echo ladder → adopt best → cache) EVALUATED via receiving-code-review: sound engineering but solves neither evidenced blocker (no rendezvous answer when Pico remembers 1M and PC opens elsewhere; no re-hunt story) and its core (negotiated switch + guard + auto-revert) duplicates approved B §3; only true delta is the automatic ladder. Ruling: keep hunt + BREAK + B; ladder adopted as PC-side BAUD_SET repetition (no new wire types; PING/PONG already prove both directions; 2-frame/N-consecutive rules equivalent; PC-side adapter+baud memory equivalent to BCBR). Intermediate-rate auto-selection ACCEPTED as needed (owner correction: adapters vary per user; YAGNI dismissal withdrawn). Flash-lifespan ruling: ~zero impact (BCBR change-only single save on adopt; sweep/dwell/BREAK are RAM-only; ladder candidates never saved; even 100 writes/day = ~500k days on 100k-erase NOR). BAUD_SET (0x36) Pico-side IMPLEMENTED (TDD: RED at T_BAUD_SET/FX_BAUD_SET undef, GREEN 4/4 + STATUS-ACK vector): dispatch range 0-4 + 0x16 reject + immediate ACT_SEND_STATUS at old rate; Core1-executed switch (DMA/UART ownership) with 100ms guard + 2s revert via poll-time compare per B §3 + 2-consecutive adopt hardening (vs B §3 first-frame; fluke-save guard) + revert log line; same-rate requests skipped. Builds exit 0: log/pico-bcon-baudhunt.uf2 SHA 74A09A5B…FA96E2 (wireless/1M) + log/pico-bcon-baudhunt-115200.uf2 SHA DF0C673A…75EF8ED (wired/115200). poc_send gained --break_ + --probe-ladder/--probe-need/--probe-cands (HW-UNVERIFIED, owner absent). Next HW (on return): B5-retry (sweep capture despite live session) + BREAK re-hunt (1M lock → --break_ 0.1 at 115200 → auto re-lock) + ladder full run (--probe-ladder from rendezvous).
- Rumble unparked (capture 2026-09-18 + T3-T6 + T9, Commit C): Temp hci_dump UF2 (2311c8d+3 lines, log/pico-bcon-hcidump-a2-10-2311c8d.uf2 SHA 53012c24) + UART0 LOG + split UART1-TX STATE injection (60-frame A bursts, BCON frames 10->11->71->131->191->251->311 landing proof). Vibration screen odd-press rule; episodes 20:33:56 (P3) / 20:37:35 (P5) / 20:43:21 (P6, predicted) each mid-sym ramp (00 45 40 52 x2) -> strong (80 00 60 92 x2). Vectors: neutral L/R/sym + non-zero L-mid/R-mid/both-mid/both-strong (4 distinct, 3 levels). Extract: log/rumble_A2_extract.txt. Gate PASS (>=3 distinct, S6.1 real bytes). Impl: T3 src/proto/rumble.h (HF spike idx formula + LF neutral-relative ((M[3]-0x40)*255+41)/82 normalized to loudest observed 0x92, per-motor max(); 0x10-entry &report[1] size>=9) + tests/host/test_rumble.c (V0 + vectors); T4 hid intake probe_rumble_l/r; T5 dispatch v3_rumble_tick + ACT_SEND_RUMBLE enum-end append + sent_* advance-guard; T6 flush arm PONG->RUMBLE->PLAYER_INFO; T9 spec v4 (S4 0x22 sending + 0x23 row, S5.4 RESULT 0x00/0x02, S5.7 rewrite + vector table, new S5.8 PLAYER_INFO, history 4.0). Verify: host 6/6 (protocol/usb/config/baud/pokecon/rumble), FW exit 0 (log/pico-bcon-rumble.uf2 SHA 5C25D31E). Temp reverted: bootsel UF2 reflashed via T_BOOTSEL, clean BCON boot (t=20s+, pm=0, no HCI flood, hci_dump 0 in tree). Notes: LF scale clamps louder-than-0x92 future captures at 255 (re-normalize if needed); USB rumble stays count-only; odd/even toggle scope uncertain (global model contradicted by P4-quiet/P5-episode vs interleave claim; per-controller best-fit); device body color persisted RED (C-1 COLOR_SET test, rewrite before color baseline checks).
- Baud-hunt HW re-run VERDICT (agent-driven serial 2026-09-18, baudhunt UF2s rebuilt from 2a3057c: wireless-1M SHA 58a1efa3 + wired-115200 derated SHA 3c9548ad; split single-adapter wiring RX<-UART0-TX + TX->UART1-RX, both 115200): B5 PASS (post-boot Switch auto-reconnect cid=1 with sweep CYCLING 1H->3H->0H->1H->2H despite session = gate-removal fix proven live; 20xPING burst @115200 -> 'data baud=115200 locked' + BCON baud=0L, frames 3->23). BREAK PASS (1M... actually 115200-locked -> serial BREAK 0.1s -> 'data baud re-hunt' + sweep restart 3H->0H->1H despite cid=1 -> PING bursts -> re-lock 'data baud=115200 locked' + baud=0L; 3 alignment misses before hit, 25%/try as designed). LADDER PASS (LOG-driven variant: BAUD_SET at current rate + PING blast at target + BCON-state verdict, since PONGs invisible on split wiring): run1 idx3=1M ADOPTED (frames 63->94, baud=3L); run1 idx2/idx1/idx0 FAILED = script bug (BAUD_SET sent at wrong TX baud, FW innocent, frames frozen at 94); run2 (fixed) idx2=921600 ADOPTED (94->125, 2L) + idx1=460800 ADOPTED (125->157, 1L) + idx0=115200 ADOPTED (157->189, 0L). Full chain 115200->1M->921600->460800->115200 with cid=1 throughout, pm=0, no resets. Transcripts: log/COM3_2026_09_18.baud_rerun.txt + log/COM3_2026_09_18.baud_ladder.txt + log/COM3_2026_09_18.baud_ladder2.txt. Device ends locked 115200 healthy. No code changes (verdict-only).
- Color/PLAYER_INFO HW CONFIRM (agent-driven serial 2026-09-18, baudhunt wireless UF2 2a3057c build, UART1-full single adapter; decoder log/poc_send_023.patch applied: poc_send.py + test_poc_send_023.py 8/8 GREEN): C-1 PASS (COLOR_SET red then blue accepted, no 0x10-reject, err stays 0x03); C-3 PASS (red body visible on Switch UI); C-4 PASS (deregister->reconnect shows BLUE body = cache re-read works per spec S12); C-6 PASS (left dark-gray vs right white distinct); P-1 PASS (PLAYER_INFO lamp=0x01 flags=0x03 reproduced on two FW builds); P-2 PASS (player-1 matches Switch UI); P-5 PASS (STATUS_REQ always piggybacks one 0x23 by design [test 10]; spontaneous quiet when unchanged = change-only dedup live); link HELLO_ACK 04000100 + PONG 3/3 + STATUS 1Hz steady. Partial (untested, documented): P-3/P-4 IMU/vibration flag transitions (need Switch SUBs), P-6 reconnect-clear. Transcripts: uart1_verify run + log/COM3_2026_09_18.color_pi.txt + blue write. Note: device color left BLUE (rewrite to default gray if a neutral baseline is needed).
